<footer id="kontak" class="bg-dark text-light py-4 mt-5">
    <div class="container text-center">
        <p class="mb-1 fw-semibold">{{ $systemName }}</p>
        <p class="mb-1">{{ $studentName }} | NIM {{ $studentNim }}</p>
        <p class="mb-1">{{ $course }}</p>
        <small>&copy; {{ $year }} {{ $studentName }} ({{ $studentNim }}) - {{ $course }}</small>
    </div>
</footer>